export const usageText = `/**
 * Usage
 * 
 * Import a module via:
 *  import 'https://cdn.jsdelivr.net/npm/lodash';
 * 
 * Use module:
 *  _.map([1, 2, 4], v => v ** 2);
 * 
 * Log output via:
 *  vm.log({ foo: 'bar' });
 * 
 * Hit \`ctrl-Enter\` to execute the code
 */

vm.log({ hello: 'world!' });
`;